#include "main.h"
/**
 * puts_half - prints half of a string
 * @str: input
 * Return: length
 */
void puts_half(char *str)
{
	int longi = 0;

	while (*s != '\0')
	{
		longi++;
		s++;
	}
	return (longi);
}
