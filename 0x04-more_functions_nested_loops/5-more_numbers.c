#include "main.h"
/**
 * more_numbers - prints 10 times the numbers
 * void
 * Return: Always 0
 */
void more_numbers(void)
{
	char i, j;

	for (i = '0'; i <= '14'; i++)
	{
		for (j = '0'; j <= '14', j++)
			_putchar(j);
	}


}
