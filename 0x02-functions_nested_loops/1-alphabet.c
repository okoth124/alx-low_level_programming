include "main.h"
/**
 * main - Entry point.
 * Decription - program that prints alphabet, lowercase.
 * Return: Always 0 (Success.
 */
int main(void)
{
	void print_alphabet(void)
	{
		char g = 'a';

		while (g <= 'z')
		{
			_putchar(g);
			g++;
		}
		_putchar('\n')
	return (0);
	}
}
