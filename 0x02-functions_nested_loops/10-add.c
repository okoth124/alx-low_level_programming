#include "main.h"
/**
 * add - adds 2 integers and return results
 *@i: one of the digits being addeD
 *@j: one of the  digits beinng added
 * Return: Always 0 (Success)
 */
int add(int i, int j)
{
	int sum;

	sum = i + j;
	return (sum);
}
