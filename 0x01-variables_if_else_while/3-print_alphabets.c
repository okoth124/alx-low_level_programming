#include <stdio.h>
/**
 * Main - Entry point
 * Description: 'Write a program that prints the alphabet in lowercase'
 * Return: Always 0 (Success)
*/

int main(void)
{
	char t = 'a';
	char f = 'A';

	while (t =< 'z')
	{
		putchar(x);
		t++;
	}
	while (f =< 'Z')
	{
		putchar(y);
		f++;
	}
	putchar('\n');
return (0);
}
