#include <stdio.h>
/**
 * Main - Entry point
 * Return: Always 0 (Success)
*/

int main(void)
	char _c = 'a';
	char _d = 'A';

	while (_c = < 'z'){
		putchar(x);
	       }
	while (_d = < 'Z'){
		putchar(y);
	       }
	putchar('\n');
	return (0);
