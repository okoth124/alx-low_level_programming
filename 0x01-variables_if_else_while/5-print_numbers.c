#include <stdio.h>
/**
 * Main - Entry point
 * Description: program prints all digit numbers of base 10 starting frm 0
 * Return always 0
 */

int main(void)
{
	int h;

	for (h = 0; h <= 9; h++)
	printf("%d", h);
	printf("\n");
	return (0);
}
